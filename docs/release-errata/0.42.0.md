# Home Center 0.42.0 — provenance erratum

Status: prepared non-destructive correction for the already-published public release `v0.42.0`.

The immutable public tag `v0.42.0` resolves to public stable commit:

`006cbf824c2a3a894a98d1619daadd0029c636b5`

The authoritative approved canonical source for this public release is:

`980e9d84736b64a6e1558bae524e4da2a1c8115a`

The authoritative stable release-boundary revision recorded by `APPROVED-SOURCE.json` is:

`c9a9c2ab5c5ccd76369409aed5d0b906bafb77b7`

The authoritative approved source manifest digest is:

`f82f1d80515fab27f320a69341e1cd137154faf6ad0281c2bd5349bd5c002958`

## What is being corrected

`RELEASE.md` inside the immutable `v0.42.0` tag contains three provenance identifiers carried forward from the prior 0.41 release cycle. Those prose values are stale and must not be used as release identity evidence.

The tagged `APPROVED-SOURCE.json`, the actual annotated tag resolution, and the published immutable release artifacts remain the authoritative evidence for the 0.42.0 release identity. Existing tag and release assets must not be moved, recreated, overwritten, or force-updated.

## Non-destructive handling

The repository `main` documentation was forward-corrected in commit `56cdc49631511771a39fb87e950238f139849807`. This erratum exists to make the immutable-tag discrepancy explicit without rewriting release history.

For future releases, checked-in documentation must not hard-code a final public tag commit SHA because a commit cannot reliably contain its own final identity. Final public tag resolution belongs in post-publication release evidence (release manifest / acceptance metadata / GitHub Release metadata) and must be verified against the actual annotated tag.
